# Onglet Pomodoro Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter un onglet Pomodoro (durée travail / pause courte / pause longue, alarme sonore, enchaînement auto) avec un affichage live du compte à rebours dans la menu bar macOS.

**Architecture:** Une classe `@Observable PomodoroTimer` (état + logique + `Timer` 1s), instanciée une fois dans `ThingsCloneApp` et injectée via `.environment()`. Un nouveau cas `SidebarSelection.pomodoro` route vers une nouvelle vue `PomodoroView`. Une scène `MenuBarExtra` lit la même instance pour l'affichage live.

**Tech Stack:** SwiftUI, `Observation` (`@Observable`), `Foundation.Timer`, `AppKit.NSSound`, XCTest.

## Global Constraints

- Durées par défaut : travail 25 min, pause courte 5 min, pause longue 15 min (spec §1).
- Pause longue déclenchée toutes les `4` sessions de travail complétées, non configurable (spec §1, hors périmètre).
- Alarme = son uniquement (`NSSound.beep()`), pas de notification macOS (spec, validé avec l'utilisateur).
- Enchaînement des phases entièrement automatique (spec, validé avec l'utilisateur).
- Pas de persistance de l'état du minuteur entre lancements de l'app (spec, hors périmètre).
- Un changement de durée ne s'applique qu'au prochain démarrage de la phase concernée, jamais en cours de compte à rebours (spec §1).
- **Ce répertoire n'est pas un dépôt git** : aucune étape "commit" dans ce plan. Chaque tâche se termine par une vérification `swift build` / `swift test` à la place.

---

### Task 1: `PomodoroTimer` — modèle et logique de phases

**Files:**
- Create: `Sources/ThingsClone/Models/PomodoroTimer.swift`
- Modify: `Package.swift`
- Test: `Tests/ThingsCloneTests/PomodoroTimerTests.swift`

**Interfaces:**
- Produces: `enum PomodoroPhase: Equatable { case work, shortBreak, longBreak }` avec `var label: String`.
- Produces: `final class PomodoroTimer` (`@Observable`) — propriétés `phase: PomodoroPhase`, `remaining: TimeInterval`, `isRunning: Bool`, `completedWorkSessions: Int` (lecture seule), `workMinutes: Int`, `shortBreakMinutes: Int`, `longBreakMinutes: Int` ; méthodes `start()`, `pause()`, `reset()`, `advancePhase()`.

- [ ] **Step 1: Ajouter le target de tests dans `Package.swift`**

Remplacer le contenu de `Package.swift` par :

```swift
// swift-tools-version: 5.10
import PackageDescription

let package = Package(
    name: "ThingsClone",
    platforms: [.macOS(.v14)],
    products: [
        .executable(name: "ThingsClone", targets: ["ThingsClone"])
    ],
    targets: [
        .executableTarget(name: "ThingsClone"),
        .testTarget(name: "ThingsCloneTests", dependencies: ["ThingsClone"])
    ]
)
```

- [ ] **Step 2: Écrire le test qui échoue (enchaînement des phases)**

Créer `Tests/ThingsCloneTests/PomodoroTimerTests.swift` :

```swift
import XCTest
@testable import ThingsClone

final class PomodoroTimerTests: XCTestCase {
    func testAdvancePhase_triggersLongBreakEveryFourthWorkSession() {
        let timer = PomodoroTimer()

        for _ in 0..<3 {
            timer.phase = .work
            timer.advancePhase()
            XCTAssertEqual(timer.phase, .shortBreak)
        }

        timer.phase = .work
        timer.advancePhase()
        XCTAssertEqual(timer.phase, .longBreak, "la 4e session de travail doit déclencher une pause longue")

        timer.advancePhase()
        XCTAssertEqual(timer.phase, .work, "une pause (courte ou longue) revient toujours au travail")
    }
}
```

- [ ] **Step 3: Lancer les tests pour vérifier l'échec**

Run: `swift test`
Expected: FAIL — `PomodoroTimer` n'existe pas encore (erreur de compilation).

- [ ] **Step 4: Implémenter `PomodoroTimer`**

Créer `Sources/ThingsClone/Models/PomodoroTimer.swift` :

```swift
import Foundation
import Observation
import AppKit

enum PomodoroPhase: Equatable {
    case work
    case shortBreak
    case longBreak

    var label: String {
        switch self {
        case .work: return "Travail"
        case .shortBreak: return "Pause courte"
        case .longBreak: return "Pause longue"
        }
    }
}

@Observable
final class PomodoroTimer {
    static let sessionsBeforeLongBreak = 4

    private static let workMinutesKey = "pomodoroWorkMinutes"
    private static let shortBreakMinutesKey = "pomodoroShortBreakMinutes"
    private static let longBreakMinutesKey = "pomodoroLongBreakMinutes"

    var phase: PomodoroPhase = .work
    var remaining: TimeInterval
    var isRunning = false
    private(set) var completedWorkSessions = 0

    var workMinutes: Int {
        didSet { UserDefaults.standard.set(workMinutes, forKey: Self.workMinutesKey) }
    }
    var shortBreakMinutes: Int {
        didSet { UserDefaults.standard.set(shortBreakMinutes, forKey: Self.shortBreakMinutesKey) }
    }
    var longBreakMinutes: Int {
        didSet { UserDefaults.standard.set(longBreakMinutes, forKey: Self.longBreakMinutesKey) }
    }

    @ObservationIgnored private var timer: Timer?

    init() {
        let defaults = UserDefaults.standard
        let work = defaults.object(forKey: Self.workMinutesKey) as? Int ?? 25
        workMinutes = work
        shortBreakMinutes = defaults.object(forKey: Self.shortBreakMinutesKey) as? Int ?? 5
        longBreakMinutes = defaults.object(forKey: Self.longBreakMinutesKey) as? Int ?? 15
        remaining = TimeInterval(work * 60)
    }

    func start() {
        guard !isRunning else { return }
        isRunning = true
        timer = Timer.scheduledTimer(withTimeInterval: 1, repeats: true) { [weak self] _ in
            self?.tick()
        }
    }

    func pause() {
        isRunning = false
        timer?.invalidate()
        timer = nil
    }

    func reset() {
        pause()
        phase = .work
        completedWorkSessions = 0
        remaining = duration(for: .work)
    }

    func advancePhase() {
        switch phase {
        case .work:
            completedWorkSessions += 1
            phase = completedWorkSessions.isMultiple(of: Self.sessionsBeforeLongBreak) ? .longBreak : .shortBreak
        case .shortBreak, .longBreak:
            phase = .work
        }
        remaining = duration(for: phase)
    }

    private func tick() {
        guard remaining > 0 else { return }
        remaining -= 1
        if remaining <= 0 {
            NSSound.beep()
            advancePhase()
        }
    }

    private func duration(for phase: PomodoroPhase) -> TimeInterval {
        switch phase {
        case .work: return TimeInterval(workMinutes * 60)
        case .shortBreak: return TimeInterval(shortBreakMinutes * 60)
        case .longBreak: return TimeInterval(longBreakMinutes * 60)
        }
    }
}
```

- [ ] **Step 5: Lancer les tests pour vérifier le succès**

Run: `swift test`
Expected: PASS — `testAdvancePhase_triggersLongBreakEveryFourthWorkSession` passe.

---

### Task 2: Entrée "Pomodoro" dans la sidebar

**Files:**
- Modify: `Sources/ThingsClone/Models/SidebarSelection.swift`
- Modify: `Sources/ThingsClone/Views/SidebarView.swift`

**Interfaces:**
- Consumes: rien de `PomodoroTimer` (juste la navigation).
- Produces: `SidebarSelection.pomodoro` (nouveau cas, sans valeur associée) — utilisé par Task 3.

- [ ] **Step 1: Ajouter le cas `pomodoro` à `SidebarSelection`**

Dans `Sources/ThingsClone/Models/SidebarSelection.swift`, remplacer :

```swift
enum SidebarSelection: Hashable {
    case smartList(SmartList)
    case project(Project)
    case area(Area)
}
```

par :

```swift
enum SidebarSelection: Hashable {
    case smartList(SmartList)
    case project(Project)
    case area(Area)
    case pomodoro
}
```

- [ ] **Step 2: Vérifier que le projet compile (switches non exhaustifs attendus)**

Run: `swift build`
Expected: FAIL — `TaskListView.swift` a des `switch` sur `SidebarSelection` non exhaustifs (traité en Task 3).

- [ ] **Step 3: Ajouter la ligne "Pomodoro" dans la sidebar**

Dans `Sources/ThingsClone/Views/SidebarView.swift`, ajouter cette propriété juste avant `private var projectsGroup: some View {` :

```swift
private var pomodoroRow: some View {
    VStack(alignment: .leading, spacing: 1) {
        row(isSelected: selection == .pomodoro) {
            selection = .pomodoro
        } label: {
            Label {
                Text("Pomodoro")
            } icon: {
                Image(systemName: "timer")
                    .foregroundStyle(.red)
            }
            .font(.system(size: 14, weight: .semibold))
        }
    }
}
```

Puis, dans `body`, remplacer :

```swift
            VStack(alignment: .leading, spacing: 28) {
                group(topLists)
                group(timeLists)
                group(archiveLists)
                projectsGroup
            }
```

par :

```swift
            VStack(alignment: .leading, spacing: 28) {
                group(topLists)
                group(timeLists)
                group(archiveLists)
                pomodoroRow
                projectsGroup
            }
```

- [ ] **Step 4: Build (échec attendu, corrigé en Task 3)**

Run: `swift build`
Expected: FAIL — toujours les `switch` non exhaustifs de `TaskListView.swift`. C'est normal, corrigé à la tâche suivante.

---

### Task 3: `PomodoroView` + routing dans `TaskListView`

**Files:**
- Create: `Sources/ThingsClone/Views/PomodoroView.swift`
- Modify: `Sources/ThingsClone/Views/TaskListView.swift`

**Interfaces:**
- Consumes: `PomodoroTimer` (Task 1) via `@Environment(PomodoroTimer.self)` — doit être injecté par l'appelant (Task 4).
- Consumes: `SidebarSelection.pomodoro` (Task 2).
- Produces: `struct PomodoroView: View` et `struct PomodoroMenuBarView: View` — `PomodoroMenuBarView` réutilisée par Task 4.

- [ ] **Step 1: Créer `PomodoroView`**

Créer `Sources/ThingsClone/Views/PomodoroView.swift` :

```swift
import SwiftUI

struct PomodoroView: View {
    @Environment(PomodoroTimer.self) private var timer

    var body: some View {
        @Bindable var timer = timer
        VStack(spacing: 24) {
            Text(timer.phase.label)
                .font(.title2)
                .foregroundStyle(.secondary)

            Text(formatted(timer.remaining))
                .font(.system(size: 64, weight: .bold, design: .rounded))
                .monospacedDigit()

            HStack(spacing: 16) {
                Button(timer.isRunning ? "Pause" : "Start") {
                    timer.isRunning ? timer.pause() : timer.start()
                }
                Button("Reset") { timer.reset() }
            }
            .buttonStyle(.bordered)

            Divider().padding(.vertical, 8)

            VStack(alignment: .leading, spacing: 12) {
                Stepper("Travail : \(timer.workMinutes) min", value: $timer.workMinutes, in: 1...120)
                Stepper("Pause courte : \(timer.shortBreakMinutes) min", value: $timer.shortBreakMinutes, in: 1...60)
                Stepper("Pause longue : \(timer.longBreakMinutes) min", value: $timer.longBreakMinutes, in: 1...60)
            }
            .frame(maxWidth: 280)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }

    private func formatted(_ interval: TimeInterval) -> String {
        let total = max(0, Int(interval))
        return String(format: "%02d:%02d", total / 60, total % 60)
    }
}

struct PomodoroMenuBarView: View {
    @Environment(PomodoroTimer.self) private var timer

    var body: some View {
        @Bindable var timer = timer
        VStack(alignment: .leading, spacing: 8) {
            Text(timer.phase.label)
                .font(.headline)
            Button(timer.isRunning ? "Pause" : "Start") {
                timer.isRunning ? timer.pause() : timer.start()
            }
            Button("Reset") { timer.reset() }
        }
        .padding(8)
    }
}
```

- [ ] **Step 2: Router `.pomodoro` dans `TaskListView.body`**

Dans `Sources/ThingsClone/Views/TaskListView.swift`, remplacer :

```swift
    var body: some View {
        if let selection {
            content(for: selection)
                .onChange(of: selection) { _, _ in resetSession() }
        } else {
            Text("Sélectionne un projet")
                .foregroundStyle(.secondary)
        }
    }
```

par :

```swift
    var body: some View {
        if let selection {
            if case .pomodoro = selection {
                PomodoroView()
            } else {
                content(for: selection)
                    .onChange(of: selection) { _, _ in resetSession() }
            }
        } else {
            Text("Sélectionne un projet")
                .foregroundStyle(.secondary)
        }
    }
```

- [ ] **Step 3: Compléter les `switch` non exhaustifs**

Dans `activeTasks(for:)`, remplacer la fin du switch :

```swift
        case .area(let area):
            return area.tasks.filter { !$0.isCompleted }.sorted { $0.sortIndex < $1.sortIndex }
        }
    }
```

(première occurrence, dans `activeTasks(for:)`) par :

```swift
        case .area(let area):
            return area.tasks.filter { !$0.isCompleted }.sorted { $0.sortIndex < $1.sortIndex }
        case .pomodoro:
            return []
        }
    }
```

Dans `visibleCompletedTasks(for:)`, remplacer :

```swift
        case .area(let area):
            completed = area.tasks.filter { $0.isCompleted && !$0.isHeader }
        }
```

par :

```swift
        case .area(let area):
            completed = area.tasks.filter { $0.isCompleted && !$0.isHeader }
        case .pomodoro:
            return []
        }
```

Dans `supportsReordering(_:)`, remplacer :

```swift
    private func supportsReordering(_ selection: SidebarSelection) -> Bool {
        switch selection {
        case .project, .area: return true
        case .smartList: return false
        }
    }
```

par :

```swift
    private func supportsReordering(_ selection: SidebarSelection) -> Bool {
        switch selection {
        case .project, .area: return true
        case .smartList, .pomodoro: return false
        }
    }
```

- [ ] **Step 4: Build pour vérifier que tout compile**

Run: `swift build`
Expected: SUCCESS (aucune vue n'est encore branchée sur `PomodoroTimer` dans l'environnement — c'est fait en Task 4, mais `PomodoroView`/`PomodoroMenuBarView` compilent indépendamment tant que `PomodoroTimer` existe dans le module).

---

### Task 4: Injection de `PomodoroTimer` + `MenuBarExtra`

**Files:**
- Modify: `Sources/ThingsClone/ThingsCloneApp.swift`

**Interfaces:**
- Consumes: `PomodoroTimer` (Task 1), `PomodoroMenuBarView` (Task 3), `ContentView` (existant).

- [ ] **Step 1: Ajouter l'état partagé, l'injection et la scène `MenuBarExtra`**

Remplacer le contenu de `Sources/ThingsClone/ThingsCloneApp.swift` par :

```swift
import AppKit
import SwiftUI
import SwiftData

@main
struct ThingsCloneApp: App {
    @State private var pomodoroTimer = PomodoroTimer()

    init() {
        NSApplication.shared.setActivationPolicy(.regular)
        NSApplication.shared.activate(ignoringOtherApps: true)
    }

    var body: some Scene {
        // Titre vide explicite : sinon SwiftUI ré-assigne "ThingsClone" (nom du bundle) à la
        // fenêtre à chaque re-render du toolbar, provoquant un flash du titre natif.
        WindowGroup("") {
            ContentView()
                .environment(pomodoroTimer)
        }
        .modelContainer(for: [TaskItem.self, Project.self, Area.self, Tag.self])

        Settings {
            SettingsView()
        }

        MenuBarExtra {
            PomodoroMenuBarView()
                .environment(pomodoroTimer)
        } label: {
            Text(menuBarLabel)
        }
    }

    private var menuBarLabel: String {
        guard pomodoroTimer.isRunning else { return "⏱" }
        let total = max(0, Int(pomodoroTimer.remaining))
        return String(format: "%02d:%02d", total / 60, total % 60)
    }
}
```

- [ ] **Step 2: Build pour vérifier que tout compile**

Run: `swift build`
Expected: SUCCESS

- [ ] **Step 3: Lancer les tests (non-régression)**

Run: `swift test`
Expected: PASS

- [ ] **Step 4: Vérification manuelle**

Run: `swift run` (ou `./run.sh` si présent)
Expected: l'app se lance, la sidebar affiche "Pomodoro" (icône minuteur), cliquer dessus affiche le compte à rebours + Start/Pause/Reset + steppers, une icône ⏱/MM:SS apparaît dans la barre de menus macOS et le clic dessus ouvre le même mini-contrôle.

## Self-Review

**Couverture spec :** §1 (modèle/logique) → Task 1 ; §2 (sidebar) → Task 2 ; §3 (vue détail) → Task 3 ; §4 (menu bar) → Task 4 ; §5 (test) → Task 1, Step 2-3-5 ; Hors périmètre (pas de persistance, son seul, auto, pas de config du seuil de 4) → respecté nativement par l'implémentation, aucune tâche ne les introduit.

**Placeholders :** aucun — code complet à chaque étape.

**Cohérence des types :** `PomodoroPhase`, `PomodoroTimer` (propriétés/méthodes) définis en Task 1 et réutilisés à l'identique en Task 3/4 ; `SidebarSelection.pomodoro` défini en Task 2, consommé en Task 2 (row) et Task 3 (routing) avec la même syntaxe `case .pomodoro`.
